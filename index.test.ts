import { WorkdayPerson } from "./index";

describe("workday-person-typescript-sdk", () => {
    it("initialize client", async () => {
        const workdayperson = new WorkdayPerson({
        });
    });
});
